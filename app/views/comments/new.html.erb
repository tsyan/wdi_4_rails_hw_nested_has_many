<h1>Create a New Comment!</h1>

<%= form_for [@user, @article, @comment] do |f| %>
	<p><%= f.label :body %></p>
	<p><%= f.text_area :body, :cols => "60", :rows => "20"  %></p>
	<p><%= f.submit %></p>
<% end %>
